.modal-dialog.modal-lg
  .modal-content
    .modal-header
      h4.modal-title #{image_tag('logo-mm.png')}
    .modal-body
      .text-center.text-primary
        |
          Paso 2 de 2.
        .what
          = "#{@coupon.promotion.done_text(@coupon)}"
        - if not @coupon.promotion.category.vehicle? and not @coupon.promotion.is_a?(BranchInformation)
          .how-much
            strong &nbsp;en #{@coupon.promotion.shops_details}
            - if @coupon.price.present?
              | &nbsp;por&nbsp;
              strong #{mismantenciones_number_to_currency(@coupon.price)}
    .modal-footer
      .row
          button.btn.btn-cta.btn-block#modal-done-button type="submit" Listo, continuar viendo mismantenciones.com