|
  $('#shop_inscription-modal').html( "#{escape_javascript(render('shop_inscription_modal'))}" );

  $.validator.addMethod("regex", function(value, element, regexpr) {
     return regexpr.test(value);
   }, '');

  $("#register_shop_inscrption").validate({
    debug: true,
    errorPlacement: function(error, element) { },
    rules: {
    "shop_inscription[name]": {required: true},
    "shop_inscription[primary_last_name]": {required: true},
    "shop_inscription[email]": {required: true, email: true},
    //"shop_inscription[rut]": {required: true, minlength: 6, regex: /^0*(\d{1,3}(\.?\d{3})*)\-([\dkK])$/},
    "shop_inscription[phone]": {required: true, digits: true, minlength: 9, maxlength: 15},
    "shop_inscription[comune_id]": {required: true},
    //"shop_inscription[accept_terms]": {required: true}
    }
  });

  $('#shop_inscription-modal').modal('show');
